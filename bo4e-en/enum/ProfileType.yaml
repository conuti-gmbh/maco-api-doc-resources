components:
  schemas:
    ProfileType:
      title: ProfileType
      type: string
      enum:
        - TYPE_STANDARD_LOAD_PROFILE
        - TYPE_DAILY_PARAMETER_DEPENDENT_LOAD_PROFILE
        - TYPE_LOAD_PROFILE
